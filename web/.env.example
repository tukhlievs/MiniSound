# URL Cloudflare Worker (без слэша в конце).
# Оставь пустым — тогда используются относительные пути (/api/tracks и т.д.),
# что правильно когда Next.js задеплоен на том же домене что и Worker.
# Заполняй только если фронт живёт на ДРУГОМ домене.
NEXT_PUBLIC_API_URL=
